<h1>New cat</h1>
<p>Tell us a bit more about <%= @cat.name %></p>

<%= form_for(@cat, :url => wizard_path, :method => :put) do |f| %>
  <%= react_component("Pagination", props: {nPages: 6, currentPage: 4, backText: 'Back', nextText: 'Next', backUrl: previous_wizard_path}, prerender: false) %>
  <%= render 'cat_errors' %>
  <p>Did you already go to the vet with <%= @cat.name %>?</p>

  <div class="row">
    <div class="col-xs-12 col-lg-8">
      <div class="field">
        <%= react_component("RadioGroup", props: {name: 'cat[vet]', items: [{label: 'Yes'}, {label: 'No'}], value: 0}, prerender: false) %>
      </div>
    </div>
    <div class="col-xs-12 col-lg-4">
      <img src="/vet.svg" class="full-width" />
    </div>
  </div>
<% end %>