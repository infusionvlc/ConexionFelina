<%= t('mailers.colony_application.approval.title', colony: @colony.name) %>

<%= t('mailers.colony_application.approval.body') %>

<%= link_to colony_url(@colony) do %>
  <%= t('shared.go') %>
<% end %>