<h1>Tasks</h1>

<%= link_to "create", new_task_path  %> </br></br>

<% @tasks.each do | task | %>
  <%= link_to task.name, task_path( task.id ) %><br/>
  <%= task.description %>
  <%= link_to "Assign this task to a user", new_task_user_wizard_path(task_id: task.id) %><br/>
  <hr/>
<% end %>